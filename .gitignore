.env

# CelebA dataset (huge — never commit; everyone downloads it locally)
celeba/
celeba.zip
*.zip

# Cached CLIP features / tensors (large binaries — extract locally via Colab notebook)
artifacts/
*.pt

# Notebook / Python cruft
.ipynb_checkpoints/
__pycache__/
*.pyc